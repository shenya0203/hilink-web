# 快速测试指南

## 前置条件

1. 已安装 Node.js (v14+) 和 npm
2. 设备 IP 地址为 `192.168.2.177`
3. 网络连接正常

## 快速启动步骤

### 第1步：安装依赖
```bash
npm install
```
这会安装以下依赖：
- Vue 3.3.4
- Vue Router 4.2.4
- Vite 4.4.9
- axios 1.4.0

### 第2步：启动开发服务器
```bash
npm run dev
```

输出示例：
```
  VITE v4.4.9  ready in 327 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

### 第3步：打开浏览器
访问 `http://localhost:5173/`

## 预期行为

### 页面加载
1. 看到导航栏（深蓝色）和标题"工业路由网关"
2. 左侧显示菜单列表
3. 主区域显示"加载中..."

### 数据加载
1. 大约 1-2 秒后，加载状态消失
2. 看到以下三个数据表格：
   - **系统信息** - 包含设备名称、固件版本、运行时间等
   - **以太网** - IP地址、连接状态等
   - **蜂窝网络** - SIM卡、信号强度等

### 功能验证

#### 测试自动刷新
- 页面会每 30 秒自动刷新一次数据
- 在浏览器开发者工具（F12）的 Network 标签中可以看到请求

#### 测试菜单导航
- 点击"网络"菜单可切换到网络配置页面
- 点击"当前状态"返回主页面

#### 测试错误处理
- 临时断开网络，关闭开发服务器，或修改设备 IP
- 页面应显示"加载数据失败"错误提示
- 恢复网络后，下次刷新应恢复正常

## 浏览器开发者工具调试

### 查看网络请求
1. 按 `F12` 打开开发者工具
2. 切换到 **Network** 标签
3. 刷新页面（F5）
4. 可以看到以下请求：
   ```
   GET /download_flex.cgi?name=status    → 代理到 192.168.2.177
   GET /download_flex.cgi?name=network   → 代理到 192.168.2.177
   GET /download_nv.cgi?name=misc        → 代理到 192.168.2.177
   ```

### 查看浏览器控制台
1. 按 `F12` 打开开发者工具
2. 切换到 **Console** 标签
3. 可以看到：
   - Vue 框架信息
   - 成功/失败的 API 调用日志
   - 任何 JavaScript 错误

### 查看 Vue 组件
1. 安装 Vue DevTools 浏览器插件
2. 在开发者工具中查看组件树
3. 实时查看和修改组件数据

## 常见问题

### Q: 页面一直显示"加载中..."
**A:** 检查以下项：
- 设备是否在线，IP 是否正确
- 网络连接是否正常
- 浏览器控制台是否有错误信息
- Vite 开发服务器是否正常运行

### Q: 显示"加载数据失败"错误
**A:** 可能原因：
- 设备 IP 地址不是 `192.168.2.177`（修改 vite.config.js）
- 设备关闭或网络不可达
- 设备的 CGI 接口不可用
- 防火墙阻止了连接

### Q: API 响应为 404
**A:** 可能原因：
- 设备不支持这些 CGI 接口
- 接口路径不正确
- 需要认证信息

### Q: 刷新后数据会重置
**A:** 这是正常的，因为：
- 组件重新挂载会重新加载数据
- 这验证了数据是动态获取的，不是硬编码的

## 修改和扩展

### 修改刷新间隔
编辑 `views/Status.vue` 或 `views/Network.vue` 的 `onMounted` 函数：
```javascript
// 改为 60 秒刷新一次
setInterval(loadData, 60000)
```

### 修改设备 IP
编辑 `vite.config.js` 中的代理配置：
```javascript
target: 'http://your-new-ip'
```

### 添加新的数据页面
1. 在 `views/` 目录创建新的 `.vue` 文件
2. 复制 Status.vue 的结构并修改
3. 在 `router/index.js` 中添加路由
4. 在 `components/Layout.vue` 中添加菜单项

## 生产构建

当开发完成后，运行：
```bash
npm run build
```

这会生成优化后的生产文件到 `dist/` 目录。

## 停止开发服务器

在终端中按 `Ctrl + C` 停止开发服务器。

## 性能优化提示

- 减少自动刷新频率以减少网络流量
- 在 Network 标签中查看请求时间
- 使用浏览器性能工具检查页面渲染时间
- 考虑添加数据缓存机制

## 获取帮助

查看以下文件获取更多信息：
- `README.md` - 项目概览
- `API_FLOW.md` - API 数据流详解
- `vite.config.js` - 代理和构建配置
