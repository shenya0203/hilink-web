这是一个优化后的 Markdown 格式文档，对工业路由器的配置参数进行了结构化整理，便于阅读和查阅。

---

# 工业路由器配置参数说明

本文档详细说明了工业路由器的各项配置参数，包括串口、网络套接字（Socket）、MQTT、云服务、边缘计算及协议转换等模块。

## 1. 串口配置 (UART)

*   **配置文件位置**：`/etc/config/uart`

### Uart1 & Uart2 配置参数

| 参数项 | 示例值 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `enable` | `1` | `1` | 串口使能（1: 启用, 0: 禁用）。页面无对应配置，默认开启。 |
| `name` | `Uart1` | - | 串口名称，对应配置顺序。 |
| `work_mode` | `2` / `1` | `2` | 工作模式。页面无对应配置。 |
| `baud_rate` | `57600` | `57600` | 波特率 (如 57600, 230400)。 |
| `data_bit` | `8` | `8` | 数据位。 |
| `stop_bit` | `1` | `1` | 停止位。 |
| `parity` | `0` | `0` | 校验位。 |
| `pack_len` | `1460` | `1460` | 帧打包长度。 |
| `pack_time` | `0` | `0` | 帧打包时间。 |
| `func` | `1` | - | 功能位。页面无对应配置。 |
| `select` | `0` | - | 选择位。页面无对应配置。 |
| `device` | `/dev/ttyS0` | `/dev/ttyS0` | 设备文件路径 (Uart1对应 `/dev/ttyS0`, Uart2对应 `/dev/ttyS1`)。 |

---

## 2. Socket 配置

*   **配置文件位置**：`/etc/config/comm_tunnel`

### 通用配置

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `enable` | `1` | Socket 使能 (0: 禁用, 1: 启用)。 |
| `name` | `SOCKA` | Socket 通道名称。 |
| `mode` | `0` | 工作模式 (0: TCP Client, 1: TCP Server, 2: UDP Client, 3: HTTP Client)。 |

### TCP Client 模式参数 (`tcpc_` 开头)

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `tcpc_server_ip` | `192.168.0.201` | 服务器 IP 或域名。 |
| `tcpc_local_port` | `0` | 本地端口 (0表示系统随机选择)。 |
| `tcpc_server_port` | `8234` | 服务器端口。 |
| `tcpc_dns_timeout` | `30` | DNS 解析超时时间 (当 server_ip 为域名时有效)。 |
| `tcpc_reconn_interval`| `5` | 重连间隔 (秒)。 |
| `tcpc_ssl_mode` | `0` | SSL 模式 (0: 不使用, 1: 使用 TLS1.2)。 |
| `tcpc_ssl_verify` | `0` | 证书认证 (0: 不认证, 1: 认证服务器证书, 2: 双向认证)。 |
| `tcpc_ssl_server_name`| `null` | 服务器证书名称 (存放目录 `/etc/config/tcpc/`)。 |
| `tcpc_ssl_client_name`| `null` | 客户端证书名称。 |
| `tcpc_ssl_client_key` | `null` | 客户端证书密钥。 |
| `tcpc_regp_en` | `0` | 注册包使能 (0: 禁用, 1: 启用)。 |
| `tcpc_regp_fmt` | `0` | 注册包内容 (0: MAC, 1: IMEI, 2: SN, 3: 自定义)。 |
| `tcpc_regp_ctx` | `` | 自定义注册包内容 (当 fmt 为 3 时有效)。 |
| `tcpc_regp_tim` | `0` | 注册包发送方式 (0: 建立连接时, 1: 发送数据时, 2: 都发送)。 |
| `tcpc_hrtp_en` | `0` | 心跳包使能 (0: 禁用, 1: 启用)。 |
| `tcpc_hrtp_fmt` | `0` | 心跳包内容 (0: MAC, 1: IMEI, 2: 自定义)。 |
| `tcpc_hrtp_ctx` | `` | 自定义心跳包内容 (当 fmt 为 2 时有效)。 |
| `tcpc_hrtp_tim` | `60` | 心跳包发送间隔 (秒)。 |

### TCP Server 模式参数 (`tcps_` 开头)

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `tcps_local_port` | `8029` | TCP Server 监听的本地端口。 |
| `tcps_conn_max_num` | `4` | 最大连接数。 |
| `tcps_timeout_handling`| `0` | 超出连接数处理 (0: KEEP 保持现有, 1: KICK 踢掉旧连接)。 |

### UDP Client 模式参数 (`udpc_` 开头)

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `udpc_server_ip` | `192.168.20.21` | 服务器 IP 或域名。 |
| `udpc_local_port` | `0` | 本地端口 (0表示系统随机)。 |
| `udpc_server_port` | `1593` | 服务器端口。 |
| `udpc_dns_timeout` | `30` | DNS 解析超时时间。 |
| `udpc_ip_port_verify` | `0` | IP/端口验证 (0: 不验证, 1: 验证来源)。 |

### HTTP Client 模式参数 (`httpc_` 开头)

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `httpc_mode` | `0` | 模式 (0: HTTP, 1: HTTPS)。 |
| `httpc_url` | `/1.php?` | 请求 URL 路径。 |
| `httpc_header` | `Accept:text/html`| 请求头信息。 |
| `httpc_cut_header` | `1` | 是否去除头部 (Cut Header)。 |
| `httpc_server_ip` | `test.usr.cn` | 服务器 IP 或域名。 |
| `httpc_server_port` | `80` | 服务器端口。 |
| `httpc_resp_timeout` | `10` | 响应超时时间。 |
| `httpc_local_port` | `0` | 本地端口。 |

---

## 3. MQTT 配置

*   **配置文件位置**：`/etc/config/comm_tunnel`
*   **配置块**：`config MQTT`

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `enable` | `1` | MQTT 使能 (0: 禁用, 1: 启用)。 |
| `name` | `MQTT1` | 通道名称。 |
| `mott_Ver` | `4` | 协议版本 (3: MQTT 3.1, 4: MQTT 3.1.1)。 |
| `client_id` | `test` | 客户端 ID。 |
| `server_ip` | `192.168.0.201` | MQTT Broker IP。 |
| `server_port` | `1883` | MQTT Broker 端口。 |
| `local_port` | `0` | 本地端口。 |
| `keepalive` | `60` | 保持连接时间 (秒)。 |
| `reconn_space` | `5` | 重连间隔 (秒)。 |
| `clean_session` | `0` | 清除会话 (0: 不清除, 1: 清除)。 |
| `conn_verify` | `0` | 连接验证 (0: 不验证, 1: 验证)。 |
| `conn_user_name`| `test` | 用户名。 |
| `conn_password` | `test` | 密码。 |
| **遗嘱设置** | | |
| `will_flag` | `0` | 遗嘱标志 (0: 无, 1: 有)。 |
| `will_topic` | `test` | 遗嘱主题。 |
| `will_msg` | `test` | 遗嘱消息。 |
| `will_qos` | `0` | 遗嘱 QoS (0, 1, 2)。 |
| `will_retention`| `0` | 遗嘱保留 (0: 不保留, 1: 保留)。 |
| **SSL 设置** | | |
| `ssl_mode` | `1` | SSL 模式 (0: 不使用, 1: 使用)。 |
| `ssl_verify` | `0` | 证书认证 (0: 不认证, 1: 认证服务器, 2: 双向认证)。 |
| `ssl_server_name`| `test` | 服务器证书文件名。 |
| `ssl_client_name`| `test` | 客户端证书文件名。 |
| `ssl_client_key` | `test` | 客户端密钥文件名。 |

---

## 4. 私有云 (Cloud) 配置

*   **配置文件位置**：`/etc/config/comm_tunnel`
*   **配置块**：`config cloud`

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `enable` | `0` | Cloud 使能 (0: 禁用, 1: 启用)。 |
| `name` | `Cloud` | 云配置名称。 |
| `pvt_deploy_enable` | `0` | 私有部署使能 (0: 禁用, 1: 启用)。 |
| `server_ip` | `192.168.0.201` | 私有云 IP 地址。 |
| `server_port` | `8234` | 私有云端口。 |

---

## 5. 边缘计算配置

### 5.1 全局配置
*   **配置文件位置**：`/etc/config/edge`

| 参数项 | 示例值 | 说明 |
| :--- | :--- | :--- |
| `enable` | `1` | 网关边缘计算使能 (0: 禁用, 1: 启用)。 |
| `refresh_frequency`| `100` | 刷新频率 (ms)。 |
| `calc_period` | `100` | 计算周期 (ms)。 |
| `poll_interval` | `100` | 轮询间隔 (ms)。 |

### 5.2 数据采集配置
*   **配置文件位置**：`/etc/config/device/points.csv`
*   **格式**：CSV 格式，包含版本行 (V)、从机配置行 (SC)、采集点行 (C)。

#### 文件头 (V 行)
`V,V1.0,N7X0,;`
*   索引 0: `V` (版本标识)
*   索引 1: `V1.0` (文件格式版本)
*   索引 2: `N7X0` (适配设备型号)

#### 从机配置 (SC 行)
示例：`SC,Device1,Device1,2,1,100,0,0,192.168.0.21:2100,Device1,;`

| 索引 | 示例值 | 含义 | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | `SC` | 标识 | Slave Config |
| 1 | `Device1` | 配置名称 | 列表显示的名称 |
| 2 | `Device1` | 详细信息 | 配置的描述 |
| 3 | `2` | 协议类型 | 1: Modbus RTU, 2: Modbus TCP |
| 4 | `1` | 从机地址 | Slave ID / Unit ID |
| 5 | `100` | 采集周期 | 单位 ms (超时时间) |
| 6 | `0` | - | 未知/保留 |
| 7 | `0` | 合并采集 | 0: 关闭, 1: 开启 |
| 8 | `192.168.x`| 通讯地址 | TCP为IP:Port，串口留空 |
| 9 | `Device1` | 设备名称 | RTU时为 UARTx，TCP时同配置名 |

#### 采集点配置 (C 行)
示例：`C,Device1,node0101,node0101,1,4,0,0,0,0,0,,400001,11,0,0,200,0,0,,;`

| 索引 | 示例值 | 含义 | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | `C` | 标识 | Collection Point |
| 1 | `Device1` | 所属从机 | 关联 SC 行的名称 |
| 2 | `node0101` | 数据点名称 | 唯一标识 |
| 3 | `node0101` | 描述信息 | 点位描述 |
| 4 | `1` | 数据类型 | 1:Bit, 4:Unsigned, 5:Signed, 6-11:32Bit各类组合, 18:Bool |
| 5 | `4` | 小数位数 | 0-6 |
| 6-10| `0` | - | 未知/保留 |
| 11 | `=%s+1` | 采集公式 | 支持四则运算，如 `=(%s+10)/%s` |
| 12 | `400001` | 寄存器地址 | Modbus 地址 |
| 13 | `11` | Bit 位数 | 当类型为 Bit 时指定具体位数 |
| 14-15| `0` | - | 未知/保留 |
| 16 | `200` | 超时时间 | 默认 200ms |
| 17 | `0` | 变化上报 | 0: 关闭, 1: 开启 |
| 18 | `0` | 变化范围 | 变化阈值 |
| 19 | `=%s+10` | 控制公式 | 云端下发数据转换公式 |

---

## 6. 数据上报配置

### 6.1 上报参数
*   **配置文件位置**：`/etc/config/device/edge_report` (JSON 格式)
*   **模板文件位置**：`/etc/config/device/template/Report1.json`

```json
{
    "link": "MQTT1",              // 对应通信接口 (如 MQTT1, MQTT2, SOCKA)
    "retention": 0,               // MQTT Retain 标志
    "err_info": "",               // 自定义错误信息
    "tmpl_file": "/template/Report1.json", // 引用上报模板文件路径
    "change_report_type": 0,      // 变化上报类型 (0:关闭, 1:开启)
    "name": "Report1",            // 配置名称
    "topic": "/UploadTopic",      // MQTT 发布主题
    "data_report_type": 0,        // 数据格式 (0:源类型, 1:转为字符串)
    "qos": 0,                     // MQTT QoS
    "err_enable": 0,              // 是否开启错误填充
    "cond": {                     // 上报条件
        "period": 5,              // 周期上报 (0:关闭, 非0:秒数)
        "timed": {                // 定时上报
            "type": 0,            // 0:关闭, 1:整时, 2:整刻, 3:整分, 4:固定时间
            "hh": 0,              // 小时
            "mm": 0               // 分钟
        }
    }
}
```

### 6.2 上报模板示例 (JSON)
定义了上报数据的结构，键值对应采集点名称。
```json
{
  "device01": {
    "node0101": "node0101",
    "node0102": "node0102"
  },
  "time": "sys_local_time"
}
```

---

## 7. 协议转换配置

### 7.1 转换参数 (JSON)
*   **配置文件位置**：`/etc/config/device/edge_access`

```json
{
    "enable": 1,            // 0: 关闭, 1: 使能
    "proto": 1,             // 0: JSON格式, 1: ModBus TCP
    "down": {               // 云端下发 (订阅)
        "link": "MQTT1",    // 通道
        "topic": "/SubTopic",
        "qos": 0
    },
    "up": {                 // 设备上报 (发布)
        "link": "MQTT1",    // 通道
        "topic": "/PubTopic",
        "qos": 0
    }
}
```

### 7.2 映射点配置 (CSV)
*   **配置文件位置**：`/etc/config/device/edge_proto_access`

#### 协议设置 (S 行)
`S,1,6,10,ModBusTCP`

| 索引 | 示例值 | 含义 | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | `S` | 标识 | Setting (设置) |
| 1 | `1` | 站地址 | Modbus 站号 |
| 2 | `6` | 32位整序 | 6 代表 ABCD (UI枚举值) |
| 3 | `10` | 32位浮点序 | 10 代表 ABCD (UI枚举值) |
| 4 | `ModBusTCP` | 协议类型 | 0: JSON, 1: ModBus TCP |

#### 任务配置 (J 行)
`J,0,1,group1,1,MQTT1,/PubTopic,0,0,MQTT1,/SubTopic,0`

| 索引 | 示例值 | 含义 | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | `J` | 标识 | Job (任务) |
| 1 | `0` | - | 保留参数 |
| 2 | `1` | 使能 | 任务开关 |
| 3 | `group1` | 组名 | 页面配置组名称 |
| 4 | `1` | 协议类型 | 0: JSON, 1: ModBus TCP |
| 5 | `MQTT1` | 上行通道 | 发布通道 |
| 6 | `/PubTopic`| 上行主题 | 发布 Topic |
| 7 | `0` | 上行 QoS | 发布 QoS |
| 8 | `0` | Retain | MQTT 保留标志 |
| 9 | `MQTT1` | 下行通道 | 订阅通道 |
| 10 | `/SubTopic`| 下行主题 | 订阅 Topic |
| 11 | `0` | 下行 QoS | 订阅 QoS |

#### 映射点 (C 行)
`C,Device1_state,Device1,18,100001`

| 索引 | 示例值 | 含义 | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | `C` | 标识 | Config (配置点) |
| 1 | `Device1_state`| 点位名称 | 内部采集点标识 |
| 2 | `Device1` | 来源设备 | 关联的采集设备 |
| 3 | `18` | 数值类型 | 同采集点类型 (18=Bool, 4=Unsigned等) |
| 4 | `100001` | 映射地址 | 对外暴露的 Modbus 寄存器地址 |

云对接
1. HLK云增加分流系统对接
   使用业务系统
2. 本地设备文件导出到云端
3. 上下行数据通道和格式
    上报通道 数据格式

    控制通道 数据格式


