# 产测通信协议文档 v1.0

## 通信基础

| 项目 | 说明 |
|---|---|
| 传输层 | TCP |
| 服务端（上位机）端口 | 998 |
| 数据格式 | JSON + `\n` 换行符分隔 |
| 编码 | UTF-8 |
| 报文最大长度 | 无限制（接收方按 JSON 闭合或换行符分割） |
| ACK 超时 | 2秒 |
| 测试结果超时 | 默认 60 秒（串口测试 5 秒） |

## 通信交互流程

```
设备                                 上位机
  |                                    |
  |--- device_register -------------->|  ① 设备注册（携带硬件信息）
  |                                    |
  |<--- test_net ---------------------|  ② 下发测试指令
  |--- ACK -------------------------->|  ③ 设备确认收到
  |--- test_net result -------------->|  ④ 返回测试结果
  |                                    |
  |<--- test_wifi --------------------|  ⑤ 下一项测试...
  |--- ACK -------------------------->|
  |--- test_wifi result ------------->|
  |                                    |
  ...（按顺序完成所有测试项）...
  |                                    |
  |<--- test_wdog --------------------|  ⑥ 看门狗测试
  |--- ACK -------------------------->|
  |--- (设备硬件重启)                  |
  |                                    |
  |--- device_register -------------->|  ⑦ 重启后重新注册（同一MAC）
  |                                    |  上位机识别为重连，看门狗测试通过
  |<--- write_tuple ------------------|  ⑧ 所有测试通过后，下发五元组写入
  |--- ACK -------------------------->|
  |--- write_tuple result ----------->|  ⑨ 返回写入结果
```

## 测试管线顺序

1. `fetch_sn` — 上位机从云端获取五元组数据（上位机内部行为，与设备无关）
2. `test_net` — 网口测试
3. `test_wifi` — WiFi 测试
4. `test_lte` — LTE/SIM 卡测试
5. `test_serial` — 串口回环测试
6. `test_led` — LED 测试（需人工确认）
7. `test_wdog` — 看门狗测试（设备重启后重连验证）
8. `write_tuple` — 写入五元组（仅当所有前置测试通过后执行）

---

## 一、设备注册（device_register）

**方向**：设备 → 上位机

**触发时机**：设备启动后，TCP 连接建立成功立即发送

### 请求格式

```json
{
    "cmd": "device_register",
    "mac": "00:11:22:33:44:55",
    "imei": "860123456789012",
    "iccid": "89861122334455667788",
    "imsi": "460011234567890",
    "modem_status": "OK",
    "flashid": "unique_flash_id_string"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `cmd` | string | 是 | 固定值 `"device_register"` |
| `mac` | string | 是 | MAC 地址，格式 `XX:XX:XX:XX:XX:XX`，从 `/dev/mtd2` 偏移 0x4 读取 |
| `imei` | string | 是 | LTE 模组 IMEI，通过 AT+CGSN 获取 |
| `iccid` | string | 是 | 内置 SIM 卡 ICCID，通过 AT+ICCID 获取 |
| `imsi` | string | 是 | 内置 SIM 卡 IMSI，通过 AT+CIMI 获取 |
| `modem_status` | string | 是 | LTE 模组初始化状态 |
| `flashid` | string | 是 | Flash 唯一 ID，从 `/proc/unique_id` 读取 |

### `modem_status` 可能值

| 值 | 含义 |
|---|---|
| `"OK"` | LTE 模组正常，SIM 卡就绪，已获取到 ICCID/IMSI |
| `"USB_ERROR"` | 未检测到 4G 模组 USB 设备（ID 19d1:1003） |
| `"SIM_NOT_READY"` | 模组 USB 存在，但切换内置卡槽后 SIM 卡未就绪 |
| `"SLOT_SWITCH_ERROR"` | 卡槽切换验证失败 |
| `"PORT_ERROR"` | 无法打开 AT 串口 `/dev/ttyUSB3` |

### 处理规则

- 上位机以 `mac` 作为设备唯一标识
- 同一 `mac` 重复注册视为重连（看门狗重启场景）

---

## 二、ACK 确认（ack）

**方向**：设备 → 上位机

**触发时机**：设备收到任何支持的测试指令后，**必须在 2 秒内**回复 ACK

### 格式

```json
{
    "cmd": "ack",
    "status": "received",
    "ref_cmd": "原指令的cmd名称"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `cmd` | string | 是 | 固定值 `"ack"` |
| `status` | string | 是 | 固定值 `"received"` |
| `ref_cmd` | string | 是 | 对应原始指令的 `cmd` 值，用于上位机匹配 |

### 可能的返回值

| 情况 | 结果 |
|---|---|
| 设备成功收到命令 | `{"cmd":"ack","status":"received","ref_cmd":"xxx"}` |
| 超时未收到 ACK | 上位机判定通信异常，该测试项标记为 Fail，停止后续测试 |

---

## 三、网口测试（test_net）

### 3.1 请求（上位机 → 设备）

```json
{"cmd": "test_net"}
```

### 3.2 响应（设备 → 上位机）

#### 成功

```json
{
    "cmd": "test_net",
    "result": "pass"
}
```

#### 失败

```json
{
    "cmd": "test_net",
    "result": "fail",
    "code": "LAN_SPEED_ERROR,WAN_SPEED_ERROR",
    "logs": "LAN_SPEED_ERROR | WAN_SPEED_ERROR"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `cmd` | string | 固定值 `"test_net"` |
| `result` | string | `"pass"` 或 `"fail"` |
| `code` | string | 失败时的错误码，多个错误码以逗号 `,` 分隔 |
| `logs` | string | 失败时的可读描述，多个以 `\|` 分隔 |

### 3.3 错误码定义

| 错误码 | 检测项 | 判定条件 |
|---|---|---|
| `LAN_SPEED_ERROR` | LAN 口物理层 | `swconfig dev switch0 port 1 show` 中不包含 `speed:100baseT` |
| `WAN_SPEED_ERROR` | WAN 口物理层 | `swconfig dev switch0 port 0 show` 中不包含 `speed:100baseT` |
| `WAN_IP_ERROR` | WAN 口网络层 | `ifconfig eth0.2` 中未获取到 IPv4 地址（DHCP 失败） |

### 3.4 所有可能的响应组合

| result | code | logs | 含义 |
|---|---|---|---|
| `"pass"` | 无 | 无 | 全部通过 |
| `"fail"` | `"LAN_SPEED_ERROR"` | 对应描述 | 仅 LAN 速率异常 |
| `"fail"` | `"WAN_SPEED_ERROR"` | 对应描述 | 仅 WAN 速率异常 |
| `"fail"` | `"WAN_IP_ERROR"` | 对应描述 | 仅 WAN 未获取 IP |
| `"fail"` | `"LAN_SPEED_ERROR,WAN_SPEED_ERROR"` | 组合描述 | LAN + WAN 速率异常 |
| `"fail"` | `"LAN_SPEED_ERROR,WAN_IP_ERROR"` | 组合描述 | LAN 速率异常 + WAN 无 IP |
| `"fail"` | `"WAN_SPEED_ERROR,WAN_IP_ERROR"` | 组合描述 | WAN 速率异常 + WAN 无 IP |
| `"fail"` | `"LAN_SPEED_ERROR,WAN_SPEED_ERROR,WAN_IP_ERROR"` | 组合描述 | 全部异常 |

---

## 四、WiFi 测试（test_wifi）

### 4.1 请求（上位机 → 设备）

```json
{"cmd": "test_wifi"}
```

支持自定义 SSID/密码/RSSI 阈值：

```json
{
    "cmd": "test_wifi",
    "ssid": "xuxu",
    "psk": "12345678",
    "rssi_threshold": -70
}
```

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `cmd` | string | 是 | — | 固定值 `"test_wifi"` |
| `ssid` | string | 否 | `"xuxu"` | 目标 WiFi SSID |
| `psk` | string | 否 | `"12345678"` | WiFi 密码 |
| `rssi_threshold` | int | 否 | `-70` | RSSI 判定阈值，单位 dBm |

### 4.2 响应（设备 → 上位机）

#### 成功

```json
{
    "cmd": "test_wifi",
    "result": "pass",
    "rssi": -65
}
```

#### 失败

```json
{
    "cmd": "test_wifi",
    "result": "fail",
    "code": "WEAK_SIGNAL",
    "rssi": -80,
    "logs": "RSSI: -80 < -70"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `cmd` | string | 固定值 `"test_wifi"` |
| `result` | string | `"pass"` 或 `"fail"` |
| `rssi` | int | 信号强度中位数值（5次采样，取中位数），单位 dBm，pass/fail 均返回 |
| `code` | string | 失败时的错误码 |
| `logs` | string | 失败时的详细描述（可选） |

### 4.3 错误码定义

| 错误码 | 含义 | 说明 |
|---|---|---|
| `DRIVER_ERROR` | WiFi 驱动异常 | iw/wpa_supplicant 工具缺失，或 mt7603e 驱动加载后 wlan 接口未出现 |
| `NO_AP` | 未扫描到目标 AP | `iw scan` 结果中未找到指定的 SSID |
| `ASSOC_TIMEOUT` | 连接目标 AP 超时 | 最多重试 3 次，每次超时 15 秒，均未关联成功 |
| `WEAK_SIGNAL` | 信号强度低于阈值 | 已关联成功，但 RSSI 中位数低于 `rssi_threshold` |

### 4.4 所有可能的响应

| result | code | rssi | 含义 |
|---|---|---|---|
| `"pass"` | 无 | 数值 | WiFi 连接成功，信号达标 |
| `"fail"` | `"DRIVER_ERROR"` | `"N/A"` 或数值 | 驱动或工具问题 |
| `"fail"` | `"NO_AP"` | `"N/A"` | 未找到目标 AP |
| `"fail"` | `"ASSOC_TIMEOUT"` | `"N/A"` | 关联超时 |
| `"fail"` | `"WEAK_SIGNAL"` | 数值 | 信号强度不足 |

---

## 五、LTE 测试（test_lte）

### 5.1 请求（上位机 → 设备）

```json
{"cmd": "test_lte"}
```

### 5.2 响应（设备 → 上位机）

#### 成功

```json
{
    "cmd": "test_lte",
    "result": "pass",
    "logs": "EXT:89451234567890123456(Ready:true), INT:89459876543210987654(Ready:true)"
}
```

#### 失败

```json
{
    "cmd": "test_lte",
    "result": "fail",
    "code": "EXT_SIM_FAIL",
    "logs": "EXT:ERROR(Ready:false), INT:89459876543210987654(Ready:true)"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `cmd` | string | 固定值 `"test_lte"` |
| `result` | string | `"pass"` 或 `"fail"` |
| `code` | string | 失败时的错误码（失败时必填） |
| `logs` | string | 双卡槽状态描述，格式：`EXT:<ICCID>(Ready:<bool>), INT:<ICCID>(Ready:<bool>)` |

### 5.3 错误码定义

| 错误码 | 含义 | 判定条件 |
|---|---|---|
| `BOTH_SIM_FAIL` | 双卡槽均失败 | 外置卡槽（Slot 0）和内置卡槽（Slot 1）的 SIM 卡均未 READY |
| `EXT_SIM_FAIL` | 外置卡槽失败 | 外置卡槽（Slot 0）SIM 卡未 READY，内置卡槽正常 |
| `INT_SIM_FAIL` | 内置卡槽失败 | 内置卡槽（Slot 1）SIM 卡未 READY，外置卡槽正常 |

### 5.4 所有可能的响应

| result | code | logs 含义 |
|---|---|---|
| `"pass"` | 无 | 双卡槽均 READY，ICCID 正常 |
| `"fail"` | `"BOTH_SIM_FAIL"` | 双卡槽均未 READY |
| `"fail"` | `"EXT_SIM_FAIL"` | 仅外置卡槽失败 |
| `"fail"` | `"INT_SIM_FAIL"` | 仅内置卡槽失败 |

---

## 六、串口测试（test_serial）

### 6.1 请求（上位机 → 设备）

```json
{
    "cmd": "test_serial",
    "data": "RkFDVE9SWV9URVNUX0RBVEFfMjAyNA==",
    "baudrate": 115200
}
```

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `cmd` | string | 是 | — | 固定值 `"test_serial"` |
| `data` | string | 是 | — | 测试负载数据，Base64 编码 |
| `baudrate` | int | 否 | `115200` | 串口波特率 |

### 6.2 响应（设备 → 上位机）

#### 成功（双路回环数据一致）

```json
{
    "cmd": "test_serial",
    "result": "pass",
    "data1": "RkFDVE9SWV9URVNUX0RBVEFfMjAyNA==",
    "data2": "RkFDVE9SWV9URVNUX0RBVEFfMjAyNA=="
}
```

#### 失败（至少一路数据不一致）

```json
{
    "cmd": "test_serial",
    "result": "fail",
    "data1": "RkFDVE9SWV9URVNUX0RBVEFfMjAyNA==",
    "data2": "XXXX"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `cmd` | string | 固定值 `"test_serial"` |
| `result` | string | `"pass"` 或 `"fail"` |
| `data1` | string | 串口 P1（ttyS1/GPIO4）回传的原始数据，Base64 编码 |
| `data2` | string | 串口 P2（ttyS0/GPIO11）回传的原始数据，Base64 编码 |

### 6.3 判定规则

上位机将 `data1`、`data2` 分别与请求中的 `data` 做**精确字符串匹配**。

| data1 == 请求data | data2 == 请求data | result | 上位机显示 |
|---|---|---|---|
| ✔ | ✔ | `"pass"` | — |
| ✘ | ✔ | `"fail"` | "P1 校验失败" |
| ✔ | ✘ | `"fail"` | "P2 校验失败" |
| ✘ | ✘ | `"fail"` | "P1&P2 校验失败" |

### 6.4 所有可能的响应

| result | data1 | data2 | 含义 |
|---|---|---|---|
| `"pass"` | 与请求一致 | 与请求一致 | 双路回环正常 |
| `"fail"` | 与请求一致 | 不一致/空 | P2 回环异常 |
| `"fail"` | 不一致/空 | 与请求一致 | P1 回环异常 |
| `"fail"` | 不一致/空 | 不一致/空 | 双路回环异常 |

---

## 七、LED 测试（test_led）

### 7.1 请求（上位机 → 设备）

```json
{"cmd": "test_led"}
```

### 7.2 设备行为（无结果返回）

设备收到命令后：
1. 回复 ACK
2. 控制 3 个 LED 以 timer 模式闪烁（50ms 亮 / 50ms 灭）：
   - `system:data:data`
   - `system:net:info`
   - `system:work:status`
3. **不发送测试结果**

测试结果由产测人员目视确认后在上位机界面点击"正常"/"异常"。

### 7.3 所有可能的判定结果（上位机内部行为）

| 人工确认 | 上位机标记结果 |
|---|---|
| 点击"确认LED闪烁" | test_led = Pass |
| 点击"异常" | test_led = Fail |

---

## 八、看门狗测试（test_wdog）

### 8.1 请求（上位机 → 设备）

```json
{"cmd": "test_wdog"}
```

### 8.2 设备行为

设备收到命令后：
1. 回复 ACK
2. 关闭所有 LED：设置 LED trigger 为 `none`
3. 关闭 TCP 连接
4. 等待 5 秒
5. 触发硬件看门狗复位（内存写操作 + GPIO 拉低）

**设备不发送测试结果。**

### 8.3 上位机判定逻辑

上位机通过**跨连接的状态机**判定看门狗测试结果：

| 阶段 | 时间 | 事件 | 判定 |
|---|---|---|---|
| 发送 test_wdog | T0 | 上位机下发指令，收到 ACK | 记录 `is_rebooting = True`，`reboot_start_time = T0` |
| 设备重启 | T0 ~ T0+5s | 设备硬件复位 | — |
| 设备重连 | T0 ~ T0+100s | 设备以 **相同 MAC** 发送 `device_register` | `is_rebooting = True` → 识别为看门狗重连 |
| 重连成功 | 重连时 | 上位机检查 `elapsed = now - reboot_start_time` | `test_wdog = Pass`，显示耗时 |
| 重连超时 | T0+100s | 100 秒内未收到重连注册 | `test_wdog = Fail`，原因"规定时间内未重连" |

### 8.4 所有可能的判定结果

| 结果 | 条件 | elapsed |
|---|---|---|
| Pass | 设备在 100 秒内以同一 MAC 重新注册 | 实际耗时（秒） |
| Fail | 设备在 100 秒内未重新注册 | — |
| Fail | ACK 超时（2秒内未收到 ACK） | — |

---

## 九、写入五元组（write_tuple）

### 9.1 请求（上位机 → 设备）

```json
{
    "cmd": "write_tuple",
    "DN": "Dev_XXXX",
    "PjK": "Project_XYZ",
    "PdK": "Product_ABC",
    "PdS": "Secret_123",
    "DS": "Device_Secret_456"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `cmd` | string | 是 | 固定值 `"write_tuple"` |
| `DN` | string | 是 | 设备名称（Device Name） |
| `PjK` | string | 是 | 项目 Key（Project Key） |
| `PdK` | string | 是 | 产品 Key（Product Key） |
| `PdS` | string | 是 | 产品 Secret（Product Secret） |
| `DS` | string | 是 | 设备 Secret（Device Secret） |

### 9.2 响应（设备 → 上位机）

#### 成功

```json
{"cmd": "write_tuple", "result": "pass"}
```

写入成功后设备执行 `firstboot -y`（恢复出厂设置），终止看门狗喂狗进程，然后进入无限循环等待断电。

#### 失败

```json
{
    "cmd": "write_tuple",
    "result": "fail",
    "msg": "可选错误描述"
}
```

### 9.3 所有可能的响应

| result | msg | 含义 |
|---|---|---|
| `"pass"` | 无 | 五元组写入成功，设备准备断电 |
| `"fail"` | 无或不提供 | 五元组写入失败（`tuple-write` 命令返回非零） |
| 超时（上位机侧） | — | 写入结果超时，通信断开 |

### 9.4 前置条件检查

上位机在发送 `write_tuple` 前会检查所有前置测试项（test_net, test_wifi, test_lte, test_serial, test_led, test_wdog）。如有任意一项为 Fail，则**跳过写入五元组**，设备返回状态为"前项有失败，跳过写入"。

---

## 十、心跳保持（Keep-alive）

### 10.1 机制

测试管线全部完成后，上位机会每 5 秒向设备发送一个空行 `\n`（仅换行符），用于探测 TCP 连接是否存活。

### 10.2 说明

| 项目 | 说明 |
|---|---|
| 方向 | 上位机 → 设备 |
| 格式 | `\n`（仅一个换行符，非 JSON） |
| 频率 | 每 5 秒一次 |
| 触发时机 | 测试管线完成后（`session.is_done = True`） |
| 设备处理 | **应忽略空行/无效 JSON 行** |
| 断开检测 | 若 `sendall` 抛出异常，上位机判定连接已断开，清理会话 |

---

## 附录：超时定义汇总

| 超时类型 | 时长 | 说明 |
|---|---|---|
| ACK 超时 | 2 秒 | 上位机发送指令后等待 ACK 的超时时间 |
| 串口测试结果超时 | 5 秒 | test_serial 等待结果的超时时间 |
| 其他测试结果超时 | 60 秒 | test_net / test_wifi / test_lte 等待结果的超时时间 |
| 看门狗重连等待超时 | 100 秒 | test_wdog 后等待设备重新注册的超时时间 |
| TCP 接收超时 | 20 秒 | 上位机 `handle_client` 初始 socket 接收超时 |
| 心跳探测间隔 | 5 秒 | 完成测试后发送 `\n` 的间隔 |
